services:
  gpu-jupyter:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: nvidia_jupyter_tensorflow_workspace
    volumes:
      - .:/workspace
    ports:
      - "8888:8888"
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]
